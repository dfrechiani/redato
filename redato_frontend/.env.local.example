# Backend M3+ portal API (FastAPI). Default dev port = 8091.
NEXT_PUBLIC_API_URL=http://localhost:8091

# Server-side: cookie name pro JWT (httpOnly). Sem ":" pra evitar conflito.
REDATO_SESSION_COOKIE=redato_session
