fastapi>=0.115
uvicorn[standard]>=0.34
pydantic[email]>=2.10
python-multipart>=0.0.20
python-dotenv>=1.0
pytz>=2024.2
tenacity>=9.0
anthropic>=0.45

# WhatsApp / OCR (Fase A)
twilio>=9.4
requests>=2.32
pillow>=11.0
numpy>=1.26

# Portal (Fase B+, M1)
sqlalchemy>=2.0
alembic>=1.18
psycopg2-binary>=2.9

# Portal — auth (M3)
bcrypt>=4.2
pyjwt>=2.10

# Portal — PDF (M8). Usamos ReportLab puro (sem deps de sistema).
# WeasyPrint funcionaria também mas exige libpango/cairo.
reportlab>=4.2

# Portal — seed do jogo "Redação em Jogo" (Fase 2). xlsx parser puro
# Python — usado por scripts/seed_cartas_estruturais.py + futuro
# scripts/seed_minideck.py.
openpyxl>=3.1

# Portal — frontend importa via OpenAPI codegen quando mover pra
# CI; por enquanto, types/portal.ts é mantido manualmente.
